'use strict';

const Constants = Object.freeze({
    Operations: {
        CREATION: 'creation',
        TRANSFER: 'transfer',
        TRANSFER_TO_END_USER: 'deliver',
        UPDATE: 'update'
    }
})

module.exports = Constants;
